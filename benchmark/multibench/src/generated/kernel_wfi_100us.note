@ kernel_wfi_100us is HAND-WRITTEN — see notes in
@ benchmark/multibench/src/generated/kernel_wfi_100us_handwritten.S.
@ wake_us=100
@ The codegen does NOT replace it; this stub exists only to keep
@ the generated/ directory consistent across regenerations.
